# 实施与验收清单

日期：2026-09-18。基线：[历史审查](reviews/2026-09-18-readiness.md)、[路线图](roadmap.md)；本轮方案：[ADR 0002](decisions/0002-compact-themes-hardening.md)。

本表用于交接，不以「已写代码」代替验收通过。实施状态为 0.2.0 交付快照；逐项证明与未验证范围见 [最终验证记录](reviews/2026-09-18-v0.2.0-validation.md)，历史审查保留原貌。负责人是本轮工作流角色，不是长期维护承诺。

## 修改与新增功能

| 编号 / 来源 | 负责人 | 实施状态 | 必须提供的验收证据 |
| --- | --- | --- | --- |
| A01 / P1 导入校验及同 ID 覆盖 | storage、ui | 实现完成；验证见下方记录 | schema、类型、重复 ID、悬空分组全量校验；显示新增/覆盖预览；确认前修改目标后拒绝过期预览；JSON 往返 |
| A02 / P1 JSON/CSV 原子性 | storage、core、ui | 实现完成；验证见下方记录 | 坏行、事务中途异常均零写入；真实 IndexedDB 验证实体与队列同时回滚；文件读取失败可见 |
| A03 / P1 并发与设备标识 | storage、ui | 实现完成；验证见下方记录 | 双页面同时编辑触发明确冲突且保留输入；过期保存不复活墓碑；同时初始化设备 ID 一致；删除与分组修改也校验版本 |
| A04 / P1 跨页刷新 | storage、ui | 实现完成；验证见下方记录 | 管理页与侧栏的新增、修改、删除、使用记录可见；失焦恢复重读；不覆盖未保存表单；复制后最近使用排序更新 |
| A05 / P1 模板单次替换 | core | 实现完成；验证见下方记录 | 填写值内 token 保留字面值；重复变量、默认值与特殊变量名回归 |
| A06 / P1 CSV 契约 | core、ui | 实现完成；验证见下方记录 | 未闭合引号等畸形格式拒绝；公式前缀默认保护；原始导出有明确提示；逗号、引号、换行及 Unicode 往返 |
| A07 / P1 异步错误反馈 | ui、storage | 实现完成；验证见下方记录 | DB、配额、剪贴板、文件读取失败均可见；保存失败保留内容并可重试；未提交不得报成功 |
| A08 / P2 DB 生命周期 | storage | 实现完成；验证见下方记录 | blocked 有提示；versionchange 关闭旧连接；首次打开失败后可重试；原数据库已有数据仍可用 |
| A09 / P2 快照与队列 | storage | 实现完成；验证见下方记录 | 单事务一致快照；同实体重复变更压缩；删除意图正确保留；队列不冒充可用同步；迁移数据不丢失 |
| A10 / P2 渲染与性能 | ui、主执行者 | 实现完成；验证见下方记录 | 加载更多不重建全部已有卡片；正文预览限制 DOM 文本；100/1,000/10,000 合成数据启动、搜索、滚动实测，注明环境与边界 |
| A11 / P2 最近使用及截断 | ui、core | 实现完成；验证见下方记录 | 最近使用默认按使用时间排序；侧栏超过 120 条明确显示截断/总数或提供继续查看 |
| A12 / P2 共享表单与可访问性 | core、ui | 实现完成；验证见下方记录 | 两页共用变量字段；弹窗有标题/语义、初始焦点、Tab 约束、Esc 关闭与焦点恢复；仅键盘完成变量复制及编辑 |
| F01 / 新增精简模式 | ui、core | 实现完成；验证见下方记录 | 标题卡片三/二/单列自适应；正文等隐藏但可搜索；复制/编辑/删除及变量正常；与密度独立；切换保留筛选，重开保留选择 |
| F02 / 新增主题 | core、ui | 实现完成；验证见下方记录 | system 与 12 个命名主题 × 两种展示检查；管理页/侧栏即时同步、重开持久化；系统变化响应；旧值兼容与未知值回退；对话框、表单、焦点及选中态可辨识 |
| E01 / 工程入口与结构 | 主执行者 | 实现完成；验证见下方记录 | 原生模块按职责抽取；统一检查命令、回归测试、路径检查及 CI；无构建安装仍可用；准确记录实际运行命令 |
| E02 / 素材与打包 | 主执行者 | 实现完成；验证见下方记录 | 可复现原创几何图标及来源记录；白名单打包不含私有导出/工具缓存；解压包真实安装与入口资源验证 |

## 发布门槛与决策边界

| 门槛 | 负责人 | 当前状态 / 完成证据 |
| --- | --- | --- |
| G1 权属与许可证 | 维护者 | 待维护者确认代码权属及选择许可证；AI 不代选或宣称已获授权 |
| G2 密钥与隐私 | 主执行者 | 文本初筛及 22 文件发布包检查通过；当前没有 Git 历史可审查，不能由工作区结果代替 |
| G3 产品权限边界 | 主执行者 | 本轮维持本地管理；复核 manifest 无新增网页采集、注入或外部请求 |
| G4 第三方来源 | 主执行者、维护者 | 替换不明图标并记录来源；主题仅参考名称及需求，自行定义配色；剩余来源问题需维护者确认 |
| G5 最小可用性 | 主执行者 | 13 项真实 Chromium 流程与 3 项解压包测试通过；原生侧栏工具栏、最低版本与其他系统未验证 |
| G6 支持范围与安全渠道 | 维护者 | 待确定正式安全接收渠道及支持范围；文档可以先明确未发布状态，不虚构联系渠道 |
| 发布动作 | 维护者 | Git 初始化、提交、推送、公开发布需对应授权；本轮实施授权不自动包含上述操作 |

云同步、网页插入、嵌套目录与拖拽属于路线图后续能力，不计入本轮缺陷修复完成标准。

## 交付记录要求

最终验证记录应逐项说明：实际改动、测试命令/环境、结果、证据文件、未验证范围。至少分开列出静态检查、纯函数、模拟 IndexedDB、真实扩展、性能与人工决策。失败和缺失分别标记 FAIL 与 UNVERIFIED，不凭实现代码将项目写为「可稳定公开发布」。
