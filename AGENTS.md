# Agent 工作约定

## 项目事实

Prompt Vault 是 Manifest V3 Chrome 扩展，原生 ES modules + HTML/CSS + IndexedDB，无运行时构建或第三方运行时依赖；开发验证使用 Node.js 22+、npm 与 Playwright。扩展根目录就是本目录。

先读 [README](README.md) 和 [架构](docs/architecture.md)，涉及持久化再读 [数据契约](docs/data-model.md)。早期技术选型和功能设想不等于当前实现；缺口见 [审查](docs/reviews/2026-09-18-readiness.md)。

## 模块定位

- `manifest.json`：权限与入口；改路径时检查 manifest、HTML 资源及模块 import。
- `background.js`：默认设置、工具栏侧边栏行为、打开管理页的快捷键。
- `db.js`：数据库、CRUD、使用记录、软删除、变更队列、JSON 快照。
- `core/`：模板、CSV、输入校验与设置；`ui/`：共享弹窗、变量字段。
- `shared.js`：通用工具与核心模块转导出；`themes.css`：共享主题。
- `manager.*`：完整管理界面；`sidepanel.*`：搜索和复制界面。
- `examples/`：公开合成示例；`docs/`：项目知识与证据。

## 变更原则

1. 保持本地优先：打开、搜索、编辑不依赖网络。当前同步队列是占位能力，不能宣称已支持云同步。
2. 延续原生模块，不因目录少就引入框架或打包器。结构调整应有具体问题与回归证据。
3. 输入及导入内容按不可信数据处理，使用 `textContent` 等安全 DOM API，不执行提示词，不把正文拼接成 HTML。
4. 实体变更与对应队列写入保持事务一致性。修改 DB 版本、导入语义、软删除或字段时提供迁移说明和数据验证，不直接丢弃用户数据。
5. 跨页面并发、数据库和剪贴板失败纳入相关变更验收；数据库提交后才反馈成功。
6. 保持最小权限。增加网页读取、注入、登录、云请求或统计时，同步更新产品边界、隐私文档与验收用例。
7. 公共文档使用仓库相对链接，不包含私人会话全文、本机绝对路径、凭据、真实导出或浏览器 profile。临时材料放 `.local/` 或 `.private/`。
8. 保留无关文件和用户改动；Git 初始化、提交、推送、发布及许可证选择按用户授权范围执行。
9. 后台快捷键调用 `sidePanel.open()` 前不要 await Chrome API；需查询状态时保留原生 callback 用户手势链。修改该路径应做真实 `onCommand` 键盘验收，扩展页面按钮测试不能替代。

## 验证与交付

- 按 [验证指南](docs/testing.md) 选择检查。统一入口为 `npm run check`、`npm test`、`npm run test:e2e`；`npm run verify` 串行执行三项。安装开发依赖用 `npm ci`，扩展运行不需 build。只报告实际执行结果。
- 修改业务逻辑时增加针对实际风险的测试；涉及 Chrome API、IndexedDB 或交互还需扩展环境验证。
- 文档小改检查链接及事实即可。语法检查不能证明完整扩展运行正常。
- 历史审查不改写成新一轮通过报告。后续修复新增验证记录，更新路线图状态。
- 汇报变更、已执行检查、结果和未验证项，不承诺未经测量的「毫秒级」「万条不卡顿」。
